# Changesets

This folder holds changeset files created with `npm run changeset`.

When changesets are merged to `main`, the Release workflow opens a "Version Packages" PR. Merging that PR publishes to npm.
